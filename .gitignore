# Env
venv/
__pycache__/
*.pyc
.pytest_cache/

# Data (raw + processed; regenerated from raw via ingestion, not committed)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

# Experiment tracking / model artifacts
mlruns/
mlartifacts/
lightning_logs/
*.ckpt
*.pt

# OS / editor
.DS_Store
.vscode/
.idea/

# Stray toolchain installs that occasionally land in the repo root due to
# relative-path bugs in installer subprocess calls (RTools/CmdStan setup).
# Real installs live outside the repo, under C:\Users\<you>\.cmdstan\.
/Users/

# Claude Code project settings
.claude/
